/*chrome.browserAction.onClicked.addListener(function(tab) {
  chrome.tabs.executeScript({
    code: 'document.body.style.backgroundColor="red"'
  });
});*/
setTimeout(function(){
anchrs = document.getElementsByTagName('a').length
console.log(anchrs)
}, 3000);
